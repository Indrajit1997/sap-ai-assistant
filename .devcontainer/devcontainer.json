{
  "name": "SAP AI Assistant",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "features": {},
  "forwardPorts": [8000, 8501],
  "portsAttributes": {
    "8000": { "label": "Backend API", "onAutoForward": "silent" },
    "8501": { "label": "Streamlit UI", "onAutoForward": "notify", "visibility": "public" }
  },
  "postCreateCommand": "pip install -r backend/requirements.txt && pip install streamlit",
  "postStartCommand": "nohup bash -c 'cd /workspaces/sap-ai-assistant && uvicorn backend.main:app --host 0.0.0.0 --port 8000 > /tmp/backend.log 2>&1 &' && echo 'Backend starting on port 8000...'",
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python"],
      "settings": {
        "python.defaultInterpreterPath": "/usr/local/bin/python"
      }
    }
  }
}
